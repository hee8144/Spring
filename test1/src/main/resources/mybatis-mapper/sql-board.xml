<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.MemberMapper">
	<select id="memberLogin" parameterType="hashmap" resultType="com.example.test1.model.Member">
		SELECT *
		FROM TBL_USER
		WHERE USERID = #{id} AND PASSWORD = #{pwd}
	</select>
	
	<select id="memberCheck" parameterType="hashmap" resultType="com.example.test1.model.Member">
		SELECT *
		FROM TBL_USER
		WHERE USERID = #{id}
	</select>
	
	<insert id="addmember" parameterType="hashmap">
	
		INSERT INTO TBL_USER(USERID ,PASSWORD , NAME , ADDRESS , PHONE , GENDER , STATUS , CDATETIME , UDATETIME) 
		VALUES(#{id} , #{pwd} , #{name}, #{addr} , #{phone} , #{gender} , #{status} , SYSDATE, SYSDATE)
	</insert>
	
	<insert id="insertUserImg" parameterType="hashmap">
		INSERT INTO TBL_USER_FILE 
		VALUES(FILE_SEQ.NEXTVAL , #{userId} , #{path} , #{filename} , #{orgName} , #{size} , #{ext})
	</insert>
	
	<select id="memberList" parameterType="hashmap" resultType="com.example.test1.model.Member">
		SELECT 
			U.*,
			DECODE( BIRTH,null,'정보없음',
				SUBSTR(BIRTH,3,2) || '.' || SUBSTR(BIRTH,5,2) || '.' || SUBSTR(BIRTH,7,2) ) CBIRTH
		FROM TBL_USER U
		<if test="userId !=null AND userId !=''">
		WHERE USERID = #{id}
		</if>
	</select>
	
	<update id="cntIncrease" parameterType="hashmap">
	<!--로그인실패시 -->
		UPDATE TBL_USER
		SET CNT = CNT + 1	
		WHERE USERID = #{id}
	</update>
	
	<update id="cntInit" parameterType="hashmap">
		UPDATE TBL_USER
		SET CNT = 0
		WHERE USERID = #{id}
	</update>
</mapper>




